<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only what the system draws before the app's own UI exists (the launch splash). It picks
         this or values-night by the app's own saved light/dark choice — see MainActivity. -->
    <style name="Theme.Player" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFF2F2F1</item>
    </style>
</resources>
